#include <iostream>
#include <vector>

using namespace std;
#include "List.h"

int main(){

    List list;
    list.insert(5);
    list.insert(10);
    list.insert(15);
    list.print();

    return 0
}